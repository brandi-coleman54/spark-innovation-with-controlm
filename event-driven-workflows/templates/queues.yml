event_queues:
  - name: rmq-orders
    type: rabbitmq
    format: yaml
    connection:
      server: 10.106.9.6
      port: 5672
      vhost: vhost1
      credentials: RMQ1
    action:
      type: runorder
logging:
  level: DEBUG
  to_file: false
  filename: consumer.log
controlm:
  aapi: ${CTM_AAPI_ENDPOINT}
  token: CONTROLM_TOKEN
