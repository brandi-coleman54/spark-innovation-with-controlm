[Unit]
Description=Code Server
After=network.target
StartLimitIntervalSec=0

[Service]
Type=simple
Restart=always
RestartSec=1
User=${user}
ExecStart=/usr/bin/code-server --host 0.0.0.0 --port 8081 --auth none ${project_dir}

[Install]
WantedBy=multi-user.target
