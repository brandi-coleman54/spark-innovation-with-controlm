{
  "${policy_name}": {
    "Type": "WorkloadPolicy",
    "Description": "Throttling Agentless Jobs",
    "RunningJobs": [
      {
        "Server": "${server}",
        "Quantity": "${running_jobs}"
      }
    ],
    "Filter": {
      "JobType": "OS",
      "Host": "${agentless_host}"
    }
  }
}
